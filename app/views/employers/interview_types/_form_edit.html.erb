<%= form_for [:employers, @interview_type], remote: true do |f| %>
  <%= f.hidden_field :company_id, value: @company.id %>
  <%= f.text_field :name, class: "form-in", required: true, autofocus: true %>
<% end %>
